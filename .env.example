# Optional. Only needed for the OpenSky fallback data source.
# Get these from https://opensky-network.org → Account → API Client.
# Set them in the Vercel dashboard (Settings → Environment Variables),
# never in a file that gets committed.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
